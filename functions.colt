??---------------------------------------------------
-- functions.colt tests functions functionality
---------------------------------------------------??

def function test(n) as
    n = n * 5
    def function inside(m) as
        n+m
    end
    inside(n)
end

def function main() as
    println("test(2) should be eval to n = 2 * 5, then calling inside(n) should return n+m or 10 + 10: test(2) = ",test(2))
end
